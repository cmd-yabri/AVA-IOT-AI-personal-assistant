// Copy this file to config.h (same folder) and fill in your values.
// config.h is git-ignored so real credentials never get committed.
#pragma once

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"

// LAN address of the machine running `python manage.py runserver 0.0.0.0:8000`
#define AVA_SERVER_BASE "http://192.168.1.50:8000"
